<div class="navbar">
  <div class="left">
    <div class="logo">
      <img src="{{ media("img/logo.jpg") }}" class="logo" alt="logo">
    </div>
    <div class="title">
      <h1>Amical'Troc</h1>
    </div>
  </div>
  <div class="menu">
    <a href="{{ route("index") }}"
       class="{{ request().getUri() == "/" ? "current" :"" }}">Accueil</a>
    <a href="{{ route("sell") }}"
       class="{{ request().getUri() == "sell" ? "current" :"" }}">Vente</a>
    <a href="{{ route("barter") }}"
       class="{{ request().getUri() == "barter" ? "current" :"" }}">Troc</a>
    <a href="{{ route("location") }}"
       class="{{ request().getUri() == "location" ? "current" :"" }}">Location</a>
  </div>
  <a class="publish-button" href="{{ route("publish") }}">
    <i class="fa-regular fa-square-plus"></i>
    <span>Déposer une annonce</span>
  </a>

</div>