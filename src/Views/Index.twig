<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Accueil</title>

  {{ resource('Style/css/main.css') }}
  {{ resource('Style/css/footer.css') }}

  <!-- cdn fontawesome -->
  <script src="https://kit.fontawesome.com/1d87cf05c5.js" crossorigin="anonymous"></script>
<body>

<div class="navbar">
  <div class="left">
    <div class="logo">
      <img src="{{ media("img/logo.jpg") }}" class="logo" alt="logo">
    </div>
    <div class="title">
      <h1>Amical'Troc</h1>
    </div>
  </div>
  <div class="menu">
    <a href="#" class="current">Accueil</a>
    <a href="{{ route("sell") }}">Vente</a>
    <a href="{{ route("barter") }}">Troc</a>
    <a href="{{ route("location") }}">Location</a>
  </div>
  <a class="publish-button" href="{{ route("/publish") }}">
    <i class="fa-regular fa-square-plus"></i>
    <span>Déposer une annonce</span>
  </a>
</div>
<div class="container">
  <h1>Quel type d'annonces cherchez vous :</h1>
  <div class="options">
    <a href="{{ route("sell") }}" class="option">Vente</a>
    <a href="{{ route("barter") }}" class="option">Troc</a>
    <a href="{{ route("location") }}" class="option">Location</a>
  </div>
</div>
{% include "Components/footer.twig" %}
</body>
</html>